// Test de Bucles (While, For) e Incrementos para Frontend 
int cuenta = 0;

main() {
    
    
    // Bucle While
    while (cuenta < 10) {
        printf("%d", cuenta);
        cuenta = cuenta + 1;
    }
    
    // Bucle For con decremento
    for (i = 5; i > 0; dec(i)) {
        printf("%d", i);
    }
    
    // Bucle For clásico
    for (i = 0; i < 3; inc(i)) {
        printf("%s", "bucle");
    }
}
